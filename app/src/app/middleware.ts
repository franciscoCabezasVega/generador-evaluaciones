/**
 * ARCHIVO INACTIVO — Next.js solo ejecuta el middleware en la raíz del proyecto.
 * La lógica real de protección de rutas está en app/middleware.ts (raíz).
 * Este archivo existe para evitar confusión; no contiene lógica activa.
 */
export {};
